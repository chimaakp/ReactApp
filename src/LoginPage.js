import React from 'react';

export class LoginPage extends React.Component{
    render(){
        return(
            <div>
                <button>Login</button>
                <button>Signup</button>
            </div>

        );
    }
}